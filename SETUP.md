# Setup Laravel 12

-   laravel new laravel-setup (install project)
-   php artisan install:api --passport (install passport authorization)
-   composer require dedoc/scramble (install )
-   php artisan migrate
-   php artisan serve
